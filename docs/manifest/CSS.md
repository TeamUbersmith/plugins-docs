# CSS

`css` is an array that identifies the CSS files the plugin uses.

## Required
No

## Accepted values
`array`

## Usage
```
"css": [
	"my_plugin.css",
	"my_plugin_extra.css"
]
```

[Go back](MANIFEST.md)
